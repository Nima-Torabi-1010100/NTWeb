$("form").on("submit", function (e) {
    e.preventDefault(); var form = $(this)[0]; var formData = new FormData(form); var btn = $("#submitBtn"); btn.prop("disabled", !0).text("در حال ارسال..."); $.ajax({
        url: window.location.pathname, type: "POST", data: formData, processData: !1, contentType: !1, success: function (response) {
            if (response.success) { showPopup("✅ پیام با موفقیت ارسال شد"); $("form")[0].reset() } else { showPopup(response.message, !0) }
            btn.prop("disabled", !1).text("ارسال پیام")
        }, error: function () { showPopup(response.message, !0); btn.prop("disabled", !1).text("ارسال پیام") }
    })
}); function showPopup(message, isError = !1) { var popUp = $("#popupMessage"); popUp.text(message).removeClass("error").toggleClass("error", isError).fadeIn(300); setTimeout(function () { popUp.fadeOut(400) }, 3000) }